export const initialContent: { [key: string]: string } = {
    // "default": require('./initial-content/Default.yarn'),
    "pax": require('./initial-content/PAX2022.yarn'),
    "default": require('./initial-content/Pirate.yarn'),
}
